# council-log — bitácora de convocatorias de cheap-fanout-ultimate

Una línea por convocatoria. Sin este log no hay forma de evaluar los kill criteria del SKILL.md.

Formato:
`fecha | tema | ¿desacuerdo sustantivo? | ¿cambió tu decisión? (qué claim) | latencia Sol | latencia K3 | timeouts`

Las líneas marcadas `[SMOKE]` son pruebas de mecánica, NO convocatorias reales: **no cuentan**
para los kill criteria (que se evalúan cada ~10 convocatorias reales).

---


<!-- Este es el TEMPLATE versionado. La bitácora real vive en ~/.claude/cheap-fanout/council-log.md
     y NO se versiona: el campo "tema" describe sobre qué estabas decidiendo, y eso puede filtrar
     contexto de cliente o de proyecto en un repo público. Vive solo en tu máquina.
     Esa ruta está fuera del skill a propósito: instalado como plugin, el skill se reescribe en
     cada `plugin update` y un log guardado ahí se perdería. -->
